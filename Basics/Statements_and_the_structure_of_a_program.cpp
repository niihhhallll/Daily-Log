
// preprocessor directive
#include <iostream>

// every program needs main function
int main() {
  // function from the preprocessor directive
  std::cout << "Hello, World" << std::endl;

  // this returns 0, which in some cases mean sucess;
  return 0;
}
